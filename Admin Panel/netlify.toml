[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Optional: Uncomment if you need to handle SPA routing
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
